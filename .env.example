# ============================================================
# NOVA NS v5 — Variáveis de Ambiente
# ============================================================
# Copie este arquivo para .env e preencha os valores reais
# ============================================================

# ---------- AIDER / LLM ----------
AIDER_MODEL=flash
GEMINI_API_KEY=sua_chave_gemini_aqui

# ---------- DOCKER / POSTGRES ----------
POSTGRES_USER=ns5_admin
POSTGRES_PASSWORD=Ns5Eng2026!SecurePass
POSTGRES_DB=ns5_main
POSTGRES_PORT=5432

# ---------- REDIS ----------
REDIS_PASSWORD=Ns5Redis2026!Pass
REDIS_PORT=6379

# ---------- N8N ----------
N8N_PORT=5678
N8N_ENCRYPTION_KEY=chave-local-ns5-2026-mude-em-producao

# ---------- EVOLUTION API (WhatsApp) ----------
EVOLUTION_API_KEY=Ns5Evolution2026!ApiKey
EVOLUTION_PORT=8080

# ---------- PORTAINER ----------
PORTAINER_PORT=9000

# ---------- SUPABASE (opcional — caso use cloud) ----------
# SUPABASE_URL=https://seu-projeto.supabase.co
# SUPABASE_KEY=sua_chave_anon_aqui
# SUPABASE_SERVICE_KEY=sua_chave_service_aqui

# ---------- DATABASE LOCAL (SQLAlchemy) ----------
# Usar PostgreSQL Docker:
# DATABASE_URL=postgresql://ns5_admin:Ns5Eng2026!SecurePass@localhost:5432/ns5_main
# Usar SQLite local (padrão):
# DATABASE_URL=sqlite:///SAIDA_HYDRONETWORK/db/construdata.db

# ---------- API ----------
CONSTRUDATA_API_HOST=127.0.0.1
CONSTRUDATA_API_PORT=8787
