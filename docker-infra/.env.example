# ============================================================
# CONSTRUDATA — VARIÁVEIS DE AMBIENTE
# ============================================================
# ⚠️ COPIE ESTE ARQUIVO COMO .env E TROQUE TODAS AS SENHAS
# ============================================================

# ===================== DOMÍNIOS (VPS) =====================
# Se rodar LOCAL (Docker Desktop), deixe como está
DOMAIN=seu-dominio.com.br
ACME_EMAIL=felipe@rkengenharia.com

# ===================== POSTGRESQL =====================
POSTGRES_USER=rk_admin
POSTGRES_PASSWORD=TROQUE_ESTA_SENHA_123
POSTGRES_DB=rk_main
POSTGRES_PORT=5432

# ===================== REDIS =====================
REDIS_PASSWORD=TROQUE_ESTA_SENHA_REDIS_456

# ===================== N8N =====================
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=TROQUE_ESTA_SENHA_N8N_789
N8N_ENCRYPTION_KEY=GERE_UMA_CHAVE_UNICA_AQUI_COM_OPENSSL_RAND_HEX_32
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/
# VPS: N8N_HOST=n8n.seu-dominio.com.br
# VPS: N8N_PROTOCOL=https
# VPS: WEBHOOK_URL=https://n8n.seu-dominio.com.br/

# ===================== EVOLUTION API =====================
EVOLUTION_API_KEY=TROQUE_ESTA_API_KEY_EVOLUTION

# ===================== CHATWOOT (Opcional) =====================
CHATWOOT_SECRET_KEY=GERE_COM_OPENSSL_RAND_HEX_64

# ===================== SMTP (E-mail) =====================
SMTP_EMAIL=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app-google
SMTP_DOMAIN=gmail.com
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587

# ===================== IAs GRATUITAS =====================
# Groq (grátis) — https://console.groq.com
GROQ_API_KEY=

# Gemini (grátis) — https://aistudio.google.com/apikey
GEMINI_API_KEY=

# OpenAI (pago, opcional)
OPENAI_API_KEY=

# ===================== SUPABASE (já existente) =====================
SUPABASE_URL=https://vblfdikfobsirwpdnybw.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
