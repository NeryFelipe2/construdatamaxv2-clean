# Copie para .env no servidor e preencha. NUNCA versione o .env real.
# Gere cada senha com:  openssl rand -hex 32

POSTGRES_PASSWORD=
EVOLUTION_KEY=

# hostname publico do servidor. Sem dominio proprio use sslip.io:
# troque os pontos do IP por tracos.  IP 203.0.113.45 -> 203-0-113-45.sslip.io
HOSTNAME_PUBLICO=
